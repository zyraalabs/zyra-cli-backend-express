services:
  - type: web
    name: zyra-cli-backend
    runtime: node
    buildCommand: pnpm install && pnpm build
    startCommand: node dist/api/index.js
    envVars:
      - key: NODE_ENV
        value: production
